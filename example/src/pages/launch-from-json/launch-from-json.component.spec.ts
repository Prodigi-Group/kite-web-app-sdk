import 'reflect-metadata';

import {
    LaunchFromJsonComponent,
} from './launch-from-json.component';

const initLaunchFromJsonComponent = () => {
    return new LaunchFromJsonComponent();
};

describe('', () => {
    test('', () => {
        expect(1).toBe(1);
    });
});
