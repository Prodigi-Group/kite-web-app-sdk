import 'reflect-metadata';

import {
    LaunchWithItemsComponent,
} from './launch-with-items.component';

const initLaunchWithItemsComponent = () => {
    return new LaunchWithItemsComponent();
};

describe('', () => {
    test('', () => {
        expect(1).toBe(1);
    });
});
